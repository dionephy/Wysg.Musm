<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Wysg.Musm.BooHill.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wysg.Musm.BooHill"
    xmlns:converters="using:Wysg.Musm.BooHill"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Wysg.Musm.BooHill">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" RowDefinitions="Auto,*" Loaded="MainWindow_OnLoaded">
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        </Grid.Resources>
        <StackPanel Spacing="12" Padding="16" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="BooHill List" FontSize="24" FontWeight="SemiBold" />
                <Button Content="Open Admin" Click="OpenAdmin_Click" />
                <Button Content="Bulk Import" Click="OpenBulkImport_Click" />
            </StackPanel>
            <TextBlock Text="Filters" FontSize="18" FontWeight="SemiBold" />
            <Grid ColumnSpacing="12" RowSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Spacing="4">
                    <TextBlock Text="Cluster" />
                    <ComboBox x:Name="ClusterCombo" PlaceholderText="All" ItemsSource="{Binding Clusters}" DisplayMemberPath="Display" SelectedValuePath="ClusterId" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Column="1">
                    <TextBlock Text="Building Number" />
                    <TextBox x:Name="BuildingBox" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Column="2">
                    <TextBlock Text="Unit Number" />
                    <TextBox x:Name="UnitBox" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Column="3">
                    <TextBlock Text="Area" />
                    <TextBox x:Name="AreaBox" />
                </StackPanel>

                <StackPanel Spacing="8" Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <CheckBox x:Name="ShowSoldCheck" Content="Show sold items" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Row="1">
                    <TextBlock Text="Min Value" />
                    <TextBox x:Name="MinValueBox" InputScope="Number" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Row="1" Grid.Column="1">
                    <TextBlock Text="Max Value" />
                    <TextBox x:Name="MaxValueBox" InputScope="Number" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Row="1" Grid.Column="2">
                    <TextBlock Text="Min Rank" />
                    <TextBox x:Name="MinRankBox" InputScope="Number" />
                </StackPanel>

                <StackPanel Spacing="4" Grid.Row="1" Grid.Column="3">
                    <TextBlock Text="Max Rank" />
                    <TextBox x:Name="MaxRankBox" InputScope="Number" />
                </StackPanel>

                <StackPanel Spacing="8" Grid.Row="2" Grid.ColumnSpan="5" Orientation="Horizontal">
                    <Button Content="Apply Filters" Click="ApplyFilters_Click" />
                    <Button Content="Clear" Click="ClearFilters_Click" />
                    <TextBlock x:Name="StatusText" Margin="12,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="1" Padding="16">
            <Grid RowDefinitions="Auto,*" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid Padding="12" ColumnSpacing="8" VerticalAlignment="Center" Background="{ThemeResource LayerFillColorAltBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="ID" FontWeight="SemiBold" />
                    <Button Grid.Column="1" Content="Building" HorizontalAlignment="Stretch" Click="SortBuilding_Click" />
                    <TextBlock Grid.Column="2" Text="Unit" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="3" Text="Area" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="4" Text="Favorite" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="5" Text="Office" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="6" Text="Status" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="7" Text="Value" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="8" Text="Rank" FontWeight="SemiBold" />
                    <Button Grid.Column="9" Content="Price Range" HorizontalAlignment="Stretch" Click="SortPrice_Click" />
                    <TextBlock Grid.Column="10" Text="Actions" FontWeight="SemiBold" />
                </Grid>

                <ScrollViewer Grid.Row="1">
                    <ListView x:Name="HouseList" ItemsSource="{Binding Houses}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Expander Expanding="HouseExpander_Expanding" Collapsed="HouseExpander_Collapsed">
                                    <Expander.Header>
                                        <Grid Padding="12" ColumnSpacing="8" Background="{ThemeResource LayerFillColorDefaultBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" />
                                                <ColumnDefinition Width="110" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="80" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding HouseId}" />
                                            <TextBlock Grid.Column="1" Text="{Binding BuildingNumber}" />
                                            <TextBlock Grid.Column="2" Text="{Binding UnitNumber}" />
                                            <TextBlock Grid.Column="3" Text="{Binding Area}" />
                                            <Button Grid.Column="4" Content="{Binding FavoriteGlyph}" Click="Favorite_Click" Tag="{Binding HouseId}" />
                                             <TextBlock Grid.Column="5" Text="{Binding OfficeGroupCount}" />
                                            <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="6">
                                                <TextBlock Text="{Binding StatusText}" Foreground="DodgerBlue" Visibility="{Binding HasStatusText, Converter={StaticResource BoolToVisibility}}" />
                                                <Ellipse Width="10" Height="10" Fill="{Binding StatusCircleColor}" Visibility="{Binding ShowStatusCircle, Converter={StaticResource BoolToVisibility}}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="7" Text="{Binding ValueDisplay}" />
                                            <TextBlock Grid.Column="8" Text="{Binding RankDisplay}" />
                                            <TextBlock Grid.Column="9" Text="{Binding PriceRange}" />
                                            <StackPanel Grid.Column="10" Orientation="Horizontal" Spacing="8">
                                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}" Padding="6,4" CornerRadius="4">
                                                    <TextBlock Text="Items" />
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Expander.Header>
                                    <ItemsControl ItemsSource="{Binding ItemsForDisplayGroups}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <Border Background="{Binding StatusColor}" Padding="8" Margin="0,4,0,4" CornerRadius="4">
                                                        <TextBlock Text="{Binding Office}" FontWeight="SemiBold" />
                                                    </Border>
                                                    <ItemsControl ItemsSource="{Binding Items}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Grid Padding="12,4" ColumnSpacing="8" Background="{ThemeResource LayerFillColorDefaultBrush}">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="80" />
                                                                        <ColumnDefinition Width="120" />
                                                                        <ColumnDefinition Width="140" />
                                                                        <ColumnDefinition Width="140" />
                                                                        <ColumnDefinition Width="140" />
                                                                        <ColumnDefinition Width="*" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBlock Text="{Binding ItemId}" />
                                                                    <TextBlock Grid.Column="1" Text="{Binding PriceDisplay}" />
                                                                    <TextBlock Grid.Column="2" Text="{Binding Office}" />
                                                                    <TextBlock Grid.Column="3" Text="{Binding AddedDate}" />
                                                                    <TextBlock Grid.Column="4" Text="{Binding LastUpdatedDate}" />
                                                                    <TextBlock Grid.Column="5" Text="{Binding Remark}" TextWrapping="Wrap" />
                                                                </Grid>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Expander>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
