<Window x:Class="Wysg.Musm.LlamaClient.Views.McpConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MCP Configuration" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Edit MCP configuration JSON" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Structure: {{&quot;mcp&quot;: {{ &quot;servers&quot;: {{ &quot;name&quot;: {{ &quot;command&quot;: &quot;...&quot;, &quot;args&quot;: [ ... ], &quot;env&quot;: {{ ... }} }} }} }}" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,8"/>
            <TextBox Text="{Binding RawJson, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     TextWrapping="NoWrap"
                     Height="360"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Close" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>