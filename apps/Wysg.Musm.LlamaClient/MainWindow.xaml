<Window x:Class="Wysg.Musm.LlamaClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wysg.Musm.LlamaClient"
        xmlns:vm="clr-namespace:Wysg.Musm.LlamaClient.ViewModels"
        mc:Ignorable="d"
        Title="Llama Client - Wysg.Musm" 
        Height="800" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        Loaded="Window_Loaded"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- Converters -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:RoleToAlignmentConverter x:Key="RoleToAlignmentConverter"/>

        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#252526"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="UserMessageBrush" Color="#2D4F7C"/>
        <SolidColorBrush x:Key="AssistantMessageBrush" Color="#3C3C3C"/>

        <!-- Button Style -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1C97EA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="#CCCCCC"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="0,0,0,4"/>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="#007ACC"/>
        </Style>

        <!-- Chat Message Template -->
        <DataTemplate x:Key="ChatMessageTemplate">
            <Border Margin="8,4" Padding="12,8" CornerRadius="8"
                    MaxWidth="700"
                    HorizontalAlignment="{Binding Role, Converter={StaticResource RoleToAlignmentConverter}}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource AssistantMessageBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Role}" Value="user">
                                <Setter Property="Background" Value="{StaticResource UserMessageBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="{Binding Role}" FontWeight="Bold" Foreground="#888888" FontSize="11" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Foreground="#CCCCCC" FontSize="14"/>
                    <ProgressBar IsIndeterminate="True" Height="2" Margin="0,8,0,0"
                                 Visibility="{Binding IsStreaming, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar: Endpoint & Connection -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Content="Endpoint:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" 
                         Text="{Binding Endpoint, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource DarkTextBox}"
                         VerticalAlignment="Center"/>

                <Label Grid.Column="2" Content="Model:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <ComboBox Grid.Column="3" 
                          ItemsSource="{Binding AvailableModels}"
                          Text="{Binding SelectedModel, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          Style="{StaticResource DarkComboBox}"
                          VerticalAlignment="Center"/>

                <Button Grid.Column="4" Content="List Models" 
                        Command="{Binding ListModelsCommand}"
                        Style="{StaticResource DarkButton}"
                        Margin="8,0,0,0"/>

                <Border Grid.Column="5" Background="#2D2D2D" CornerRadius="4" Padding="12,6" Margin="8,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="#888888"/>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Parameters & Presets -->
            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <!-- Parameters Section -->
                        <TextBlock Text="PARAMETERS" FontWeight="Bold" Foreground="#888888" FontSize="11" Margin="0,0,0,12"/>

                        <Label Content="Temperature"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Slider Value="{Binding Temperature}" Minimum="0" Maximum="2" SmallChange="0.1" LargeChange="0.2" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding Temperature, StringFormat=F2}" Style="{StaticResource DarkTextBox}" TextAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>

                        <Label Content="Top P" Margin="0,12,0,0"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Slider Value="{Binding TopP}" Minimum="0" Maximum="1" SmallChange="0.05" LargeChange="0.1" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding TopP, StringFormat=F2}" Style="{StaticResource DarkTextBox}" TextAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>

                        <Label Content="Max Tokens" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding MaxTokens}" Style="{StaticResource DarkTextBox}"/>

                        <Label Content="Stop Sequences (comma-separated)" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding StopSequences, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource DarkTextBox}"
                                 AcceptsReturn="False"/>

                        <CheckBox Content="Enable Streaming" IsChecked="{Binding UseStreaming}" Margin="0,16,0,0"/>

                        <!-- Presets Section -->
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,20,0,12"/>
                        <TextBlock Text="PRESETS" FontWeight="Bold" Foreground="#888888" FontSize="11" Margin="0,0,0,12"/>

                        <ComboBox ItemsSource="{Binding Presets}"
                                  SelectedItem="{Binding SelectedPreset}"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource DarkComboBox}"
                                  Margin="0,0,0,8"/>

                        <WrapPanel>
                            <Button Content="Save" Command="{Binding SavePresetCommand}" Style="{StaticResource DarkButton}" Width="60"/>
                            <Button Content="New" Command="{Binding NewPresetCommand}" Style="{StaticResource DarkButton}" Width="60"/>
                            <Button Content="Dup" Command="{Binding DuplicatePresetCommand}" Style="{StaticResource DarkButton}" Width="50"/>
                            <Button Content="Del" Command="{Binding DeletePresetCommand}" Style="{StaticResource DarkButton}" Width="50"/>
                        </WrapPanel>

                        <!-- Actions Section -->
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,20,0,12"/>
                        <TextBlock Text="ACTIONS" FontWeight="Bold" Foreground="#888888" FontSize="11" Margin="0,0,0,12"/>

                        <Button Content="📋 Copy as JSON" Command="{Binding CopyAsJsonCommand}" Style="{StaticResource DarkButton}" HorizontalAlignment="Stretch"/>
                        <Button Content="▶️ Replay from Clipboard" Command="{Binding ReplayFromClipboardCommand}" Style="{StaticResource DarkButton}" HorizontalAlignment="Stretch"/>
                        <Button Content="🗑️ Clear Chat" Command="{Binding ClearChatCommand}" Style="{StaticResource DarkButton}" HorizontalAlignment="Stretch"/>

                        <!-- MCP Section -->
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,20,0,12"/>
                        <TextBlock Text="MCP TOOLS" FontWeight="Bold" Foreground="#888888" FontSize="11" Margin="0,0,0,12"/>

                        <CheckBox Content="Enable MCP Tools" IsChecked="{Binding EnableMcpTools}" Margin="0,0,0,8"/>

                        <ItemsControl ItemsSource="{Binding AvailableTools}" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" 
                                              IsChecked="{Binding IsEnabled}"
                                              ToolTip="{Binding Description}"
                                              Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Panel: Chat Area -->
            <TabControl Grid.Column="1" Background="{StaticResource BackgroundBrush}" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
                        <Setter Property="Foreground" Value="#CCCCCC"/>
                        <Setter Property="Padding" Value="16,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Background="{StaticResource PanelBrush}" Padding="{TemplateBinding Padding}" CornerRadius="4,4,0,0" Margin="2,0,0,0">
                                        <ContentPresenter ContentSource="Header"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- Chat Tab -->
                <TabItem Header="💬 Chat">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- System Prompt -->
                        <Border Background="{StaticResource PanelBrush}" Padding="12,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Label Content="System Prompt" FontWeight="Bold"/>
                                <TextBox Grid.Row="1" 
                                         Text="{Binding SystemPrompt, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource DarkTextBox}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         MinHeight="60"
                                         MaxHeight="120"
                                         VerticalScrollBarVisibility="Auto"/>
                            </Grid>
                        </Border>

                        <!-- Chat Messages -->
                        <ScrollViewer Grid.Row="1" 
                                      x:Name="ChatScrollViewer"
                                      VerticalScrollBarVisibility="Auto"
                                      Background="{StaticResource BackgroundBrush}">
                            <ItemsControl ItemsSource="{Binding ChatMessages}"
                                          ItemTemplate="{StaticResource ChatMessageTemplate}"/>
                        </ScrollViewer>

                        <!-- Input Area -->
                        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="InputTextBox"
                                         Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource DarkTextBox}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         MaxHeight="150"
                                         VerticalScrollBarVisibility="Auto"
                                         KeyDown="InputTextBox_KeyDown"/>

                                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,0,0">
                                    <Button Content="Send" 
                                            Command="{Binding SendMessageCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            Width="80"
                                            Visibility="{Binding IsGenerating, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    <Button Content="Stop" 
                                            Command="{Binding StopGenerationCommand}"
                                            Style="{StaticResource DarkButton}"
                                            Width="80"
                                            Background="#C42B1C"
                                            Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- MCP Tab -->
                <TabItem Header="🔧 MCP">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Model Context Protocol (MCP) configuration coming soon..." 
                                   Foreground="#888888" FontSize="14" Margin="0,20"/>

                        <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,40,0,0">
                            <TextBlock Text="MCP allows connecting to external tools and services." Foreground="#666666" TextWrapping="Wrap"/>
                            <TextBlock Text="Configure MCP servers to enable tool calling capabilities." Foreground="#666666" TextWrapping="Wrap" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- JSON Tab -->
                <TabItem Header="📄 JSON">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Last Request JSON" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,8"/>
                        <TextBox Grid.Row="1"
                                 Text="{Binding LastRequestJson, Mode=OneWay}"
                                 Style="{StaticResource DarkTextBox}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" Padding="12,6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusMessage}" Foreground="#888888"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Width="100" Height="4" 
                                 Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding SelectedPreset.Name, StringFormat='Preset: {0}'}" Foreground="#666666"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
