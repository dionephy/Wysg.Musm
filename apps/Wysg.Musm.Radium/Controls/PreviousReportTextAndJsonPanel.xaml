<UserControl x:Class="Wysg.Musm.Radium.Controls.PreviousReportTextAndJsonPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Header and Findings TextBox with ScrollViewer -->
        <ScrollViewer x:Name="PART_LeftHost" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Grid.Column="0">
            <Grid>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Previous Header and Findings" Margin="6,0,0,0"/>
                    <TextBox x:Name="txtHeaderAndFindings"
                             Text="{Binding HeaderAndFindingsText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="6" Margin="0" TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" 
                             AcceptsReturn="True"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="2" Background="#2D2D30"/>

        <!-- Right: JSON TextBox -->
        <TextBox x:Name="txtJson"
                 Text="{Binding JsonText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Column="2" 
                 Padding="6" Margin="0" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" 
                 AcceptsReturn="True"
                 VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
