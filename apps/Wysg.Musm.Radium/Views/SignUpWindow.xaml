<Window x:Class="Wysg.Musm.Radium.Views.SignUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign up" Height="300" Width="380" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Border Background="#FF2D2D30" CornerRadius="8" BorderBrush="#FF3F3F46" BorderThickness="1" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Create account" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,12"/>

            <TextBlock Grid.Row="1" Text="E-mail" Foreground="#FFAAAAAA" FontSize="10" Margin="8,0,0,2"/>
            <TextBox Grid.Row="1" Margin="0,14,0,0" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="28"
                     Background="#FF3C3C3C" Foreground="White" BorderBrush="#FF555555"/>

            <TextBlock Grid.Row="2" Text="Password" Foreground="#FFAAAAAA" FontSize="10" Margin="8,8,0,2"/>
            <PasswordBox Grid.Row="2" Margin="0,22,0,0" x:Name="pwd" Height="28" Background="#FF3C3C3C" Foreground="White"/>

            <TextBlock Grid.Row="3" Text="Display name (optional)" Foreground="#FFAAAAAA" FontSize="10" Margin="8,8,0,2"/>
            <TextBox Grid.Row="3" Margin="0,22,0,0" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Height="28"
                     Background="#FF3C3C3C" Foreground="White" BorderBrush="#FF555555"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Width="90" IsCancel="True" Margin="0,0,8,0"/>
                <Button Content="Sign up" Width="100" Command="{Binding SignUpCommand}" CommandParameter="{Binding ElementName=pwd}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
