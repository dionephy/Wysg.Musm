<UserControl x:Class="Wysg.Musm.Radium.Views.SettingsTabs.NetworkSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <!-- Database Pane -->
            <GroupBox Header="Network" Margin="0,0,0,12" Padding="8">
                <GroupBox.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource BrushTextPrimary}"/>
                    </DataTemplate>
                </GroupBox.HeaderTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Local/Intranet Connection String:" VerticalAlignment="Center" Margin="0,0,8,4"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LocalConnectionString, UpdateSourceTrigger=PropertyChanged}" MinWidth="520" Margin="0,0,0,4"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Central (Azure SQL) Connection String:" VerticalAlignment="Center" Margin="0,4,8,4"/>
                    <TextBox Grid.Row="1" Grid.Column="1" MinWidth="520" ToolTip="Paste AAD passwordless connection string (Authentication=Active Directory Default)"
                             Text="{Binding CentralConnectionString, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,4"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Snowstorm Base URL:" VerticalAlignment="Center" Margin="0,4,8,4"/>
                    <TextBox Grid.Row="2" Grid.Column="1" MinWidth="520" ToolTip="e.g. https://snowstorm.ihtsdotools.org/snowstorm/snomed-ct"
                             Text="{Binding SnowstormBaseUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,4"/>
                    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button Content="Test Local" Width="100" Margin="0,0,8,0" Command="{Binding TestLocalCommand}"/>
                        <Button Content="Test Central" Width="110" Margin="0,0,8,0" Command="{Binding TestCentralCommand}"/>
                        <Button Content="Save" Width="90" Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
