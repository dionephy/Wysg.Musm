<UserControl x:Class="Wysg.Musm.Radium.Views.SettingsTabs.PacsSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <GroupBox Header="PACS" Padding="8">
                <GroupBox.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource BrushTextPrimary}"/>
                    </DataTemplate>
                </GroupBox.HeaderTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Manage PACS profiles for different systems. Each profile has its own automation settings and UI Spy configurations." 
                               TextWrapping="Wrap" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,8"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding PacsProfiles}" SelectedItem="{Binding SelectedPacsProfile, Mode=TwoWay}"
                              AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" 
                              IsReadOnly="True" RowHeaderWidth="0" MinHeight="150" MaxHeight="300" Margin="0,0,0,8"
                              Background="{DynamicResource BrushSurface}" Foreground="{DynamicResource BrushTextPrimary}"
                              BorderBrush="{DynamicResource BrushBorder}" RowBackground="{DynamicResource BrushSurface}"
                              AlternatingRowBackground="{DynamicResource BrushSurfaceAlt}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="120"/>
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd}}" Width="100"/>
                            <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt, StringFormat={}{0:yyyy-MM-dd}}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Add PACS" Width="100" Command="{Binding AddPacsProfileCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
