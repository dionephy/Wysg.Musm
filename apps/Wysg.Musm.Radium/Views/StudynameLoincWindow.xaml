<Window x:Class="Wysg.Musm.Radium.Views.StudynameLoincWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Studyname &#8596; LOINC Parts"
        Height="600" Width="900"
        Background="#111" Foreground="#d0d0d0" FontFamily="Consolas"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#C8C8C8"/>
            <Setter Property="BorderBrush" Value="#3D3D40"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4,2,0,2"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
        </Style>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Studyname list + add -->
        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Studynames" Margin="0,0,8,0"/>
            <Button Content="Add" Command="{Binding AddStudynameCommand}"/>
        </StackPanel>
        <TextBox Grid.Column="0" Grid.Row="1" Margin="0,4,0,4" Text="{Binding StudynameFilter, UpdateSourceTrigger=PropertyChanged}"/>
        <ListBox Grid.Column="0" Grid.Row="1" Margin="0,32,0,0"
                 ItemsSource="{Binding Studynames}"
                 SelectedItem="{Binding SelectedStudyname, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Studyname}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Column="1" Grid.RowSpan="2" Width="6" Background="#2D2D30"/>

        <!-- LOINC parts by subcategory -->
        <Grid Grid.Column="2" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="LOINC Parts (grouped by rad.[subcategory])"/>
            <TreeView Grid.Row="1" ItemsSource="{Binding PartsBySubcategory}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Parts}">
                        <TextBlock Text="{Binding Subcategory}"/>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <CheckBox Content="{Binding PartDisplay}" IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                    <ComboBox DockPanel.Dock="Right" Width="60" Margin="6,0,0,0"
                                              SelectedItem="{Binding PartSequenceOrder, Mode=TwoWay}">
                                        <ComboBoxItem>A</ComboBoxItem>
                                        <ComboBoxItem>B</ComboBoxItem>
                                        <ComboBoxItem>C</ComboBoxItem>
                                        <ComboBoxItem>D</ComboBoxItem>
                                    </ComboBox>
                                </DockPanel>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>

        <GridSplitter Grid.Column="3" Grid.RowSpan="2" Width="6" Background="#2D2D30"/>

        <!-- Mapping preview -->
        <StackPanel Grid.Column="4" Grid.RowSpan="2">
            <TextBlock Text="Mapping Preview"/>
            <ListBox ItemsSource="{Binding SelectedParts}" DisplayMemberPath="PartDisplay"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}"/>
            <Button Content="Close" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>