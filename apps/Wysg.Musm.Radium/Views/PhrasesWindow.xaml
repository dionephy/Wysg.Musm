<Window x:Class="Wysg.Musm.Radium.Views.PhrasesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Phrases" Height="500" Width="640"
        Background="#1E1E1E" Foreground="#D0D0D0" FontFamily="Consolas"
        WindowStyle="None" ResizeMode="CanResize" AllowsTransparency="False">
    <Window.Resources>
        <!-- Dark theme brushes -->
        <SolidColorBrush x:Key="Brush.Panel" Color="#252526"/>
        <SolidColorBrush x:Key="Brush.PanelAlt" Color="#2D2D30"/>
        <SolidColorBrush x:Key="Brush.Border" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="Brush.Selection" Color="#094771"/>
        <SolidColorBrush x:Key="Brush.Title" Color="#2D2D30"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6 3"/>
            <Setter Property="Margin" Value="6 0 0 0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="#252526"/>
            <Setter Property="AlternatingRowBackground" Value="#1F1F20"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="0 0 1 1"/>
            <Setter Property="Padding" Value="6 4"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="0 0 0 1"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource Brush.Selection}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D0D0D0"/>
        </Style>
    </Window.Resources>
    <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" Background="#1E1E1E">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource Brush.Title}" MouseLeftButtonDown="OnDragMove">
                <TextBlock Text="Phrases" VerticalAlignment="Center" Margin="12 0 0 0" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 0 4 0">
                    <Button Width="26" Height="22" Content="_" ToolTip="Minimize" Click="OnMinimize"/>
                    <Button Width="26" Height="22" Content="?" ToolTip="Close" Click="OnClose"/>
                </StackPanel>
            </Grid>

            <!-- Input Row -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="8 6 8 6">
                <TextBox Width="280" Text="{Binding NewText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Add / Activate" Command="{Binding AddCommand}"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            </StackPanel>

            <!-- Data Grid -->
            <DataGrid Grid.Row="2" Margin="8 0 8 6" ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" IsReadOnly="False" RowHeaderWidth="0">
                <DataGrid.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Padding" Value="4 2"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="FontSize" Value="12"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Text" Binding="{Binding Text}" Width="*" IsReadOnly="True"/>
                    <DataGridCheckBoxColumn Header="Active" Width="70" Binding="{Binding Active, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt}" Width="140" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Rev" Binding="{Binding Rev}" Width="80" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Bottom Bar -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 6 8 6">
                <Button Content="Close" Width="80" Click="OnClose"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>