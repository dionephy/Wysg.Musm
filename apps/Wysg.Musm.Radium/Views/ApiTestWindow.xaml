<Window x:Class="Wysg.Musm.Radium.Views.ApiTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API Connection Test" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="API Connection Test Tool" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Status -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Status:" FontWeight="Bold"/>
            <TextBlock x:Name="StatusText" 
                       Text="Not tested yet" 
                       FontSize="14" 
                       Foreground="Gray"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Test Buttons -->
        <WrapPanel Grid.Row="2" Margin="0,0,0,20">
            <Button x:Name="QuickTestButton" 
                    Content="Quick Test" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="QuickTestButton_Click"/>
            
            <Button x:Name="FullTestButton" 
                    Content="Full Test Suite" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="FullTestButton_Click"/>
            
            <Button x:Name="ClearButton" 
                    Content="Clear Log" 
                    Width="100" 
                    Height="35" 
                    Click="ClearButton_Click"/>
        </WrapPanel>

        <!-- Log Output -->
        <Border Grid.Row="3" 
                BorderBrush="Gray" 
                BorderThickness="1" 
                Padding="10"
                Background="#F5F5F5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogText" 
                           FontFamily="Consolas" 
                           FontSize="12" 
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Instructions -->
        <TextBlock Grid.Row="4" 
                   TextWrapping="Wrap" 
                   Margin="0,15,0,0"
                   Foreground="DarkBlue">
            <Run Text="?? "/>
            <Run FontWeight="Bold" Text="Instructions: "/>
            <Run Text="1. Make sure API is running (http://localhost:5205)  "/>
            <Run Text="2. Click 'Quick Test' to verify connection  "/>
            <Run Text="3. Login first, then run 'Full Test Suite' to test all endpoints"/>
        </TextBlock>
    </Grid>
</Window>
